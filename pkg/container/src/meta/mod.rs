/*
What we need:
- The

*/

use datastore::meta::client::MetastoreClient;

pub struct ClusterMetaClient {
    meta_client: MetastoreClient,
}

impl ClusterMetaClient {
    // Need to be able to set a record.
}

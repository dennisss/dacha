extern crate common;
extern crate peripheral;

use common::errors::*;
use peripheral::ddc::DDCDevice;


fn main() -> Result<()> {


    // for i in 0..127 {
    //     if dev.test(i).is_ok() {
    //         println!("Good addr: {:2x}", i);
    //     }
    // }

    Ok(())
}
[package]
name = "macros"
version = "0.1.0"
authors = ["Dennis Shtatnov <densht@gmail.com>"]

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0.8", features = ["extra-traits", "full"] }
quote = "1.0.2"

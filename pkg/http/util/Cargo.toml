[package]
name = "http_util"
version = "0.1.0"
edition = "2018"

[dependencies]
base_util = { path = "../../base/util" }
common = { path = "../../common" }
executor = { path = "../../executor" }
file = { path = "../../file" }
http = { path = "../" }
mime_types = { path = "../../../third_party/mime_types" }

[features]
default = ["std"]
std = ["alloc"]
alloc = []
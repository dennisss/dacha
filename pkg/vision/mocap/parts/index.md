Big files are not uploaded to GitHub. See https://www.printables.com/model/1745863-optical-ir-motion-capture-camera/files

Alternatively you can download them directly into this folder by running:

```bash
cargo run --bin source_control -- fetch 'pkg/vision/mocap/parts/**/*'
```
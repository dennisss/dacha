[package]
name = "elf_tools"
version = "0.1.0"
edition = "2018"

[dependencies]
common = { path = "../../common" }
executor = { path = "../../executor" }
parsing = { path = "../../parsing" }
macros = { path = "../../macros" }
elf = { path = ".." }
file = { path = "../../file" }
[package]
name = "base_time"
version = "0.0.0"
edition = "2018"

[dependencies]
base_error = { path = "../error" }

[target.'cfg(target_os = "linux")'.dependencies]
sys = { path = "../../sys" }

[features]
default = ["std"]
alloc = []
std = ["alloc"]
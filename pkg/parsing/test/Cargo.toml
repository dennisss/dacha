[package]
name = "parsing_test"
version = "0.1.0"
edition = "2018"

[dependencies]
common = { path = "../../common" }
parsing = { path = ".." }

[build-dependencies]
parsing_compiler = { path = "../compiler" }
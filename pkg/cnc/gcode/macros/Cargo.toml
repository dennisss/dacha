[package]
name = "gcode_macros"
version = "0.0.0"

[lib]
proc-macro = true

[dependencies]
gcode_decimal = { path = "../decimal" }
syn = { version = "1.0.23", features = ["extra-traits", "full"] }
quote = "1.0.6"
proc-macro2 = "1.0.13"

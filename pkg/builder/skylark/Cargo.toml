[package]
name = "skylark"
version = "0.1.0"
edition = "2018"

[dependencies]
common = { path = "../../common" }
parsing = { path = "../../parsing" }
protobuf = { path = "../../protobuf" }
protobuf_test_proto = { path = "../../protobuf/test/proto" }
crypto = { path = "../../crypto" }
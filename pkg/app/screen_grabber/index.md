# Screenshot Grabbing App

```

cargo run --bin builder -- build //pkg/app/screen_grabber:app

cargo run --bin screen_grabber -- --port=8000 --tls_certificate=$PWD/test.crt --tls_key=$PWD/test.key


```